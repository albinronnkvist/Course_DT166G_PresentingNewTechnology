# Example data and weights
data <- c(4, 8, 6, 5, 3, 7)
weights <- c(1, 5, 1, 1, 1, 1)

# Calculate the weighted mean
weighted_mean_value <- weighted.mean(data, weights)

# Output the result
weighted_mean_value